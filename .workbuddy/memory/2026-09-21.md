# 2026-09-21

- 复刻淘宝订单详情页（交易成功）为静态网页 `index.html`（单文件 HTML+CSS+原生JS）
- 动态配置：URL 查询参数（img/title/price/address/时间等 17 个参数）+ 页面右下角⚙配置面板（localStorage 持久化，支持上传本地图片）+ DEFAULTS 对象
- 从原截图 (1179×2556) 用 Pillow 裁出商品图 `img/product.jpg` 作为默认图
- 初始化 Git 仓库并提交；README.md 内含 Render 静态站点部署步骤（Build Command 留空、Publish Directory `./`）
- 通过内置发布能力上线预览：https://47293d8b8188491499d3ad3e9f65370a.app.workbuddy.host
- 校验：JS 语法 OK、DOM id 引用完整、本地服务 200
- 环境备注：playwright-cli 已装入受管 node workspace（`~/.workbuddy/binaries/node/workspace`），但其 Chromium 在本环境崩溃，无法截图验证，改用 node 语法检查

## 第二轮：重构为「配置页 + 详情页」双页（用户反馈适配不好）

- `index.html` 改为配置页：商品图(URL/本地上传)、标题、金额、收货地址、下单时间(datetime-local)等 15 字段；桌面端右侧 iframe 实时预览（等比缩放），移动端抽屉预览；「生成订单详情」跳 order.html?params，「复制链接」生成分享链接
- `order.html` 为详情复刻页：纯 URL 参数驱动（无 localStorage），右下「✎ 编辑数据」带参回配置页
- 适配修复：输入框 16px 防 iOS 缩放、100dvh、safe-area、overflow-x、375px 小屏不溢出、日期选择器 step=1 保留秒
- 已发布更新：https://47293d8b8188491499d3ad3e9f65370a.app.workbuddy.host （index=配置页）
- Render 部署仍需用户自己的 GitHub+Render 账号，步骤在 README.md

## 第三轮：对照原图高保真还原（用户确认后已同步线上）

- 从原图裁切店铺头像 img/avatar.jpg（修正过一次坐标：头像实际在 y≈542-638）
- 样式对齐：店铺+商品同卡、规格纯灰文本、标题右侧˄+深色¥5.99、绿/灰描边标签、优惠解析粉色胶囊、礼金图标、😴 emoji、电池徽标、底部栏图标重绘
- 默认地址改为「…里水镇 里水大道南****」，物流显示「送至 里水大道南」、订单信息显示星号打码，两处均同原图
- commit c683ee2；线上已更新

## 第四轮：真实渲染对比修复（用户反馈"还是不像"）

- 关键转变：不再盲改 CSS，改用「系统 Chrome + playwright-core(channel:chrome) 真实截图 ↔ 原图并排对比」迭代；截图脚本 /tmp/shot.js（393×852 @2x fullPage），对比图拼接脚本用 PIL
- 修复：规格恢复灰底胶囊（之前误改纯文本）、标签改浅色填充（极速退款 #e8f8f1 / 7天无理由 #f5f5f5）、共减 ¥1.00→¥1（trimMoney 去零）、原价下移(side gap 18px)、标题字重 500→400、全页间距压缩（923 vs 原图 910）、默认地址改星号打码 + 新增 logi 参数独立控制「送至」片段
- 对比图存于 workspace：对比图-原图vs渲染.png；commit 2d2d514
- 经验：Mac 本机 playwright 内置 chromium 会崩，用 channel:'chrome' 走系统 Chrome 稳定

## 第五轮：图标高保真（用户反馈"图标不像"）

- 逐个放大原图裁切核对后修复 8 处：物流图标改橙色描边圆角方块+顶部U形（原为填充式）、删除物流栏灰色人形图标（原图没有）、收货信息后的😴改为闭眼弧线+睫毛 SVG（灰蓝 #4d5b6b）、客服改橙色圆圈笑脸（竖线眼+微笑弧，原误画耳机）、更多圆圈加粗 #15161c/2.3、礼金图标改橙色圆角方块+白¥（原误画礼盒）、优惠解析改白底粉描边胶囊+橙粉紫渐变文字(background-clip:text)、极速退款标签去掉绿色星星
- 物流栏「困 86-189****0880」原图是黑色粗体，改 innerHTML+<b>（带 HTML 转义防注入）
- 踩坑：同一文件多个 Edit 并行调用会互相覆盖（读-改-写竞态），只有后写的落盘；同文件编辑必须串行
- commit 132fbc4；对比图已更新

## 第六轮：文字按钮高保真（用户反馈按钮未还原）

- 裁图核对发现：原图所有文字按钮（闲鱼转卖/申请售后/加入购物车 + 底部评价/加入购物车）都是**浅灰底 #f1f3f6 / 浅橙底 #fdf0e7、无边框、圆角矩形(8-9px)**，不是白底描边胶囊
- 再买一单也是圆角矩形非胶囊，高度 37→34px
- commit 99027c8；对比图已更新

## 第七轮：店铺头像自定义（commit e97df69）

- 配置页「订单与店铺」卡片新增店铺头像字段：圆形预览 + URL 输入 + 本地上传(base64) + 使用默认头像；avatar 加入 FIELDS（实时预览/生成/复制链接自动携带）
- order.html DEFAULTS 加 avatar，render 应用到 #shop-avatar，URL 参数 avatar（别名 headimg/shopimg）
- 同文件多改动改用 python 脚本串行 replace（每处 assert count==1），规避 Edit 并发竞态

## 第八轮：上传图片优化（commit fafb6f6）

- 上传自动压缩：compressImage() Canvas 等比缩放（商品图最长边 640px q0.82 / 头像 256px q0.85）→ JPEG dataURL；3000px 测试图商品图 base64 仅 ~4KB、头像 ~2KB
- 预览图支持拖拽上传（bindDrop：img-preview/avatar-preview）；上传后 toast 反馈压缩体积；非图片文件拦截
- 端到端验证：playwright(chrome) setInputFiles → URL 参数携带 → order.html 两图渲染成功
- 测试教训：断言 URLSearchParams 内容时要注意 :// 会被编码；page.click('text=...') 会命中页面说明文字，用 'button:has-text(...)'

## 第九轮：移动端适配（commit 4bd1d35）

- 注意：order.html 被外部工具改过（底栏样式、body{height:100%} 等），修改前须先重读文件现状，勿按记忆锚点改
- 核心修复：body{height:100%} + flex 导致 .phone 高度锁死视口、内容溢出容器、padding-bottom 失效 → 底栏遮挡发货时间行；移除 height 并加 align-items:flex-start
- 底栏按钮加 white-space:nowrap（修"加入购物车"折行），≤400px 降档（图标 36px、按钮 12/13px、gap 7px）
- 配置页预览抽屉 overflow 改可滚动，≤700px 高时 shell 高度按 88dvh-70px 自适应
- 验证方法：playwright 移动视口 scrollTo(0,1e9) 后量 last row bottom vs bar.top 几何，比截图可靠（fullPage 截图 fixed 元素位置会错位，勿据其判断遮挡）

## 第八轮：推送到 GitHub 仓库

- 本地改动先 commit（10fa236），再 `git remote add origin https://github.com/Guojx123/taobooo.git` + `git push -u origin main`，推送成功，远程 main = 10fa236
- 环境要点：本机**没有 gh CLI**；`~/.ssh/id_rsa` 未被 GitHub 接受（ssh -T 报 Permission denied publickey），HTTPS 走 GCM（credential.helper=/usr/local/share/gcm-core/git-credential-manager）已存凭据 username=Guojx123，push 免密成功
- 排查技巧：`git credential fill` 可检测已存凭据；`git ls-remote <url>` 返回空且 exit 0 = 仓库存在但为空；macOS 无 `timeout` 命令
- 遗留待办：仓库含 .DS_Store(8KB) 与 .workbuddy/memory/*.md（本地工作记录），建议加 .gitignore 并 git rm --cached，已提示用户但未擅自处理
- 后续可接 Render 静态站点部署（README.md 有步骤：Build Command 留空，Publish Directory ./）

## 第十轮：修复本地图片生成报 400（commit b549661，已推送+已重新发布）

- 根因：配置页把本地上传图片的 base64（压缩后仍 40KB+）拼进跳转 URL，服务器 URI 上限约 8KB，实测 base64≥8000 字符即被拒（浏览器表现为 400，再长直接断连 000）
- 修复：index.html 新增 buildQuerySplit()——data:image 开头的值不进 URL，改存 sessionStorage('ob-big')，URL 只带 big=1 标记；updatePreview/generate/copyLink 三处全部换用；order.html getConfig() 见 big=1 时回读 sessionStorage 补缺失字段（URL 参数仍优先，分享链接跨设备回退默认图）
- 验证：curl 定位 8KB 阈值复现；playwright(chrome) 上传 960KB PNG → 压缩至 44KB base64 → 预览 iframe 正常、跳转 URL 仅 76 字符、详情页双图 naturalWidth>0、全程无 HTTP≥400；默认流程回归通过
- 注意：Bash 工具的子进程（nohup/&）在命令结束后会被回收，本地静态服务必须用 run_in_background 参数起
- 项目已配置自动提交钩子（改动会自动 commit+push），手动 commit 会显示 nothing to commit，勿误判为修改丢失
- 线上已重新发布，链接不变：https://47293d8b8188491499d3ad3e9f65370a.app.workbuddy.host

## 第十一轮：优惠解析区域高保真（commit 075da1f，已推送）

- 用户反馈「优惠解析还原的不是很好」。逐像素测量原图（原截图 3x，÷3 为 CSS px）后修 4 处：
  1. 共减¥1：玫红 13px → **橙 #ff6404、16px、600**（原图实测纯色橙，与实付款同字号）
  2. 优惠解析胶囊：高约 16px、字号 11.5px；背景与描边都是橙→粉渐变（`background: 渐变 padding-box, 渐变 border-box; border:1px solid transparent` 实现渐变边框）；文字渐变 #ff6404→#ff4255→#fd2c81（品红收尾），箭头 #fd2c81
  3. 礼金标签：加**顶部气泡尖角**（::before 三角，left:64px，宽 10 高 6px，位置实测在标签左起约 67px 处）；底色 #f3f6f8、文字 #4f5e78（蓝灰，非 #666）、12px、radius 7px、标签总宽约 104px
  4. 礼金图标：改**票券形**——橙色圆角方块左右两侧中部各有一个半圆缺口，用 SVG mask（circle 挖孔）实现
- 方法论：不猜颜色，用 PIL 扫描像素聚类取主色 + bbox 量字号（CJK 字形高 ≈ 0.87×font-size），对比图存 对比图-优惠解析.png
- 注意：本轮项目自动提交钩子未触发，手动 commit+push 成功（075da1f），说明钩子不是每次都可靠，改完要 git status 核对
